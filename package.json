{
  "name": "simple-voting",
  "version": "1.0.0",
  "description": "Simple Voting Smart Contract - Blockchain Project 2",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat ignition deploy ignition/modules/SimpleVoting.js --network localhost",
    "deploy:sepolia": "hardhat ignition deploy ignition/modules/SimpleVoting.js --network sepolia",
    "interact": "hardhat run scripts/interact.js --network localhost",
    "node": "hardhat node"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^6.1.2",
    "hardhat": "^2.28.6"
  },
  "dependencies": {
    "dotenv": "^17.4.2"
  }
}
